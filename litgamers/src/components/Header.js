import React from 'react';
import Nav from './Nav';

const Header = () => {

    return (
        <header>
            <div id="banner">
                <Nav />
            </div>
        </header>
    );
}

export default Header;